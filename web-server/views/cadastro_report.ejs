<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SIGLAB: Reportar Problema</title>
    
    <link href="/css/cadastro.css" rel="stylesheet">

    <style>
        :root {
            --bg-color: #000000;
            --main-green: #00ff41;
            --dim-green: #008F11;
            --alert-pink: #ff00ff;
            --ui-gray: #1a1a1a;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        input[readonly] {
            background-color: #111;
            border-color: #005500;
            color: #008F11;
            cursor: not-allowed;
        }
    </style>
</head>

<%- include('partials/header') %>

<body>
    <main class="main-container">

        <h2 class="titulo-pagina">Reportar Problema</h2>
        
        <div style="text-align: center; margin-bottom: 20px; color: var(--alert-pink); font-family: 'Press Start 2P', monospace; font-size: 0.6em;">
            LABORATÓRIO <%= id_lab %> >> DISPOSITIVO ID: <%= id_dispositivo %>
        </div>

        <!-- O Action deve apontar para sua rota de salvar o report -->
        <form action="/laboratorios/<%= id_lab %>/report/<%= id_dispositivo %>" method="POST" class="form-cadastro">
            
            <div class="form-group">
                <label for="lab_info">Laboratório:</label>
                <input type="text" id="lab_info" value="Laboratório <%= id_lab %>" readonly>
            </div>

            <div class="form-group">
                <label for="disp_info">Dispositivo Alvo:</label>
                <input type="text" id="disp_info" value="<%= id_dispositivo %>" readonly>
            </div>

            <div class="form-group">
                <label for="titulo">Assunto do Problema:</label>
                <input type="text" id="titulo" name="titulo" placeholder="Ex: Monitor piscando / Mouse quebrado" required autofocus>
            </div>

            <div class="form-group">
                <label for="tipo_problema">Categoria:</label>
                <select id="tipo_problema" name="tipo_problema">
                    <option value="hardware">Hardware (Peça física)</option>
                    <option value="software">Software (Sistema/Programas)</option>
                    <option value="rede">Conectividade/Internet</option>
                    <option value="outro">Outro</option>
                </select>
            </div>

            <div class="form-group">
                <label for="gravidade">Gravidade:</label>
                <select id="gravidade" name="gravidade">
                    <option value="baixa">🟢 Baixa (Estético/Leve)</option>
                    <option value="media" selected>🟡 Média (Funcionalidade Reduzida)</option>
                    <option value="alta">🔴 Alta (Inoperante)</option>
                </select>
            </div>

            <div class="form-group">
                <label for="descricao">Descrição Detalhada:</label>
                <textarea id="descricao" name="descricao" rows="6" placeholder="Descreva exatamente o que aconteceu..." required></textarea>
            </div>

            <button type="submit" class="btn-pixel" style="border-color: #000; color: #000;">
                ⚠️ ENVIAR REPORT
            </button>
            
            <a href="/laboratorios/<%= id_lab %>" style="display: block; text-align: center; margin-top: 15px; color: var(--dim-green); text-decoration: none; font-size: 0.8em;">
                [ VOLTAR PARA O MAPA ]
            </a>

        </form>

    </main>

    <%- include('partials/footer') %>
</body>
</html>