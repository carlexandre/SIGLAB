<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  
    <title>SIGLAB - Laboratório <%= lab.id %></title>
    
    <link href="/css/laboratorios.css" rel="stylesheet">
    
    <link href="https://fonts.googleapis.com/css2?family=VT323&family=Press+Start+2P&display=swap" rel="stylesheet">

    <link rel="apple-touch-icon" sizes="180x180" href="/favicon/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon/favicon-16x16.png">
    
    <link rel="manifest" href="/favicon/site.webmanifest">
    
    <link rel="shortcut icon" href="/favicon/favicon.ico">
</head>

<body>
    
    <%- include('partials/header') %> 

    <main>
        <h1 class="titulo-pagina"><%= lab.nome %></h1>

        <!-- STATUS DO LABORATÓRIO -->
        <div class="lab-status-display">
            Status: 
            <div class="lab-status-wrapper">
                <span id="labStatusBadge" 
                      class="lab-status-badge <%= lab.status || 'disponivel' %>"
                      <% if (user && user.tipo === 'admin') { %> onclick="toggleLabMenu()" <% } %>>
                    
                    <span id="labStatusText"><%= (lab.status || 'disponivel').replace('_', ' ').toUpperCase() %></span>
                    
                </span>
                
                <% if (user && user.tipo === 'admin') { %>
                    <div id="labStatusMenu" class="tooltip-lab">
                        <span class="close-status-btn" onclick="toggleLabMenu()">x</span>
                        <button onclick="changeLabStatus('disponivel')">Disponível</button>
                        <button onclick="changeLabStatus('ocupado')">Ocupado</button>
                        <button onclick="changeLabStatus('manutencao')">Manutenção</button>
                        <button onclick="changeLabStatus('inativo')">Inativo</button>
                    </div>
                <% } %>
            </div>
        </div>

        <p class="subtitulo-localizacao">
            <%= lab.localizacao %>
        </p>

        <!-- painéis de informação -->
        <div class="info-panels-container">
            
            <a href="/disponibilidade/<%= lab.id %>" class="info-card-link">
                <div class="info-card">
                    <h2>Disponibilidade do Laboratório</h2>
                </div>
            </a>
            
            <a href="/laboratorios/<%= lab.id %>/avisos" class="info-card-link">
                <div class="info-card">
                    <h2>Avisos</h2>
                </div>
            </a>

            <% if (user && user.tipo === 'admin') { %>
                <a href="/historico/<%= lab.id %>" class="info-card-link">
                    <div class="info-card">
                        <h2>Histórico</h2>
                    </div>
                </a>
            <% } %>
        </div>

        <!-- MODAL DE GALERIA DE FOTOS -->
        <div id="modalGaleria" class="modal">
            <div class="modal-content modal-large">
                <span class="close-btn" onclick="closeGalleryModal()">&times;</span>
                <h3>> Galeria_de_Fotos_</h3>

                <!-- Grid de Fotos -->
                <div class="gallery-grid">
                    <% if (lab.imagens && lab.imagens.length > 0) { %>
                        <% lab.imagens.forEach(img => { %>
                            <!-- CAMINHO: /datas/fotos/{id_lab}/{arquivo} -->
                            <div class="gallery-item" onclick="expandImage('/datas/fotos/<%= lab.id %>/<%= img %>')">
                                <img src="/datas/fotos/<%= lab.id %>/<%= img %>" alt="Foto do laboratório">
                            </div>
                        <% }); %>
                    <% } else { %>
                        <p style="grid-column: 1/-1; text-align: center; color: var(--off-gray);">
                            [ SEM IMAGENS REGISTRADAS ]
                        </p>
                    <% } %>
                </div>

                <!-- Formulário de Upload (Apenas Admin) -->
                <% if (user && user.tipo === 'admin') { %>
                    <div class="modal-divider">
                        <span>ADICIONAR NOVAS FOTOS</span>
                    </div>
                    
                    <form action="/laboratorios/<%= lab.id %>/upload" method="POST" enctype="multipart/form-data" class="modal-form">
                        <label for="fotos">Selecione as imagens:</label>
                        <input type="file" id="fotos" name="fotos" accept="image/*" multiple required>
                        <button type="submit">⬆️ REALIZAR UPLOAD</button>
                    </form>
                <% } %>
            </div>
        </div>

        <!-- MODAL PARA IMAGEM EXPANDIDA -->
        <div id="modalImagemExpandida" class="modal-image-overlay" onclick="closeExpandedImage()">
            <span class="close-image-btn">&times;</span>
            <img id="imgExpandida" src="" alt="Imagem Expandida">
        </div>

        <!-- MODAL DE CADASTRO DE DISPOSITIVO -->
        <% if (user && user.tipo === 'admin') { %>
            <div id="modalDispositivo" class="modal">
                <div class="modal-content">
                    <span class="close-btn" onclick="closeModal()">&times;</span>
                    <h3>> Gerenciar_Dispositivos_</h3>
                    
                    <!-- Cadastro Manual -->
                    <form action="/laboratorios/<%= lab.id %>/dispositivos" method="POST" class="modal-form">
                        <label style="color: #fff; border-bottom: 1px solid #005500; padding-bottom: 5px;">[ MODO MANUAL ]</label>
                        
                        <label for="nome">Nome do Dispositivo:</label>
                        <input type="text" id="nome" name="nome" placeholder="Ex: PC_01" required>
                        
                        <label for="tipo">Tipo:</label>
                        <select id="tipo" name="tipo" required>
                            <option value="" disabled selected>SELECIONE...</option>
                            <option value="computador">COMPUTADOR</option>
                            <option value="projetor">PROJETOR</option>
                            <option value="impressora">IMPRESSORA</option>
                        </select>
                        
                        <label for="status">Status Inicial:</label>
                        <select id="status" name="status" required>
                            <option value="livre">LIVRE</option>
                            <option value="em_uso">EM USO</option>
                            <option value="inoperante">INOPERANTE</option>
                        </select>

                        <button type="submit">CONFIRMAR INSERÇÃO</button>
                    </form>

                    <!-- Divisória Visual -->
                    <div class="modal-divider">
                        <span>OU IMPORTAR EM MASSA</span>
                    </div>

                    <!-- Importação CSV -->
                    <div class="import-section">
                        <!-- Input Escondido para Upload -->
                        <input type="file" id="csvFileInput" accept=".csv" style="display: none;" onchange="handleFileSelect(event)">
                        
                        <button onclick="document.getElementById('csvFileInput').click()" class="btn-action btn-import-modal">
                            📥 Carregar Arquivo CSV
                        </button>
                        <p style="font-size: 0.9rem; color: #005500; margin-top: 5px;">*Formato: id_dispositivo,nome,tipo,status</p>
                    </div>
                </div>
            </div>
        <% } %>

        <!-- container da topologia -->
        <div class="topology-container">
            <!-- HEADER DA TOPOLOGIA COM BOTÕES DE AÇÃO -->
            <div class="topology-header">
                <h2>Topologia do Laboratório</h2>
                
                <!-- Botões-->
                <div class="header-buttons">
                    
                    <!-- BOTÃO GALERIA -->
                    <button onclick="openGalleryModal()" class="btn-action btn-icon" title="Galeria de Fotos">
                        <h3>Fotos do Laboratório</h3>
                    </button>

                    <% if (user && user.tipo === 'admin') { %>
                        <!-- Botão de Editar Topologia -->
                        <button id="btnToggleEdit" onclick="toggleEditMode()" class="btn-action btn-edit-topology">
                            <h3 id="btnEditText">📐 Editar Topologia</h3>
                        </button>

                        <button onclick="openModal()" class="btn-action">
                            <h3>+ Novo Dispositivo</h3>
                        </button>
                    <% } %>
                </div>
            </div>
            
            <div class="topology-map" id="topologyMap">
                <!-- 
                  looping EJS para listar os dispositivos 
                -->
                <% if (lab.dispositivos && lab.dispositivos.length > 0) { %>
                    <% lab.dispositivos.forEach(disp => { %>
                        
                        <!-- 
                           Dispositivo com coordenadas de grid e atributos de Drag & Drop
                        -->
                        <div class="device <%= disp.status %>" 
                             id="<%= disp.id_dispositivo %>"
                             style="grid-area: <%= disp.posicao ? disp.posicao.row : '1' %> / <%= disp.posicao ? disp.posicao.col : '1' %>;"
                             data-nome="<%= disp.nome %>">
                            
                            <span class="status-label">
                                <%= disp.status.replace('_', ' ') %>
                            </span>

                            <span class="device-icon">
                                <% if (disp.tipo === 'projetor') { %>
                                    📽️
                                <% } else if (disp.tipo === 'impressora') { %>
                                    🖨️
                                <% } else { %>
                                    🖥️
                                <% } %>
                            </span>

                            <!-- Tooltip (Menu de ações e detalhes) -->
                            <div class="tooltip" id="tooltip-<%= disp.id_dispositivo %>">
                                <div class="tooltip-content">
                                    <p><strong>ID:</strong> <%= disp.nome %></p>
                                    <p><strong>Tipo:</strong> <%= disp.tipo %></p>
                                    <hr style="border-color: inherit; margin: 5px 0; opacity: 0.5;">
                                    
                                    <a href="/laboratorios/<%= lab.nome %>/report/<%= disp.id_dispositivo %>" class="tooltip-btn btn-report">Reportar Problema</a>
                                    
                                    <% if (user && user.tipo === 'admin') { %>
                                        <!-- BOTÃO ALTERAR STATUS -->
                                        <button class="tooltip-btn btn-change-status" onclick="toggleStatusMenu('<%= disp.id_dispositivo %>')">Alterar Status</button>
                                        
                                        <!-- MENU DE OPÇÕES (ESCONDIDO) -->
                                        <div class="status-options-menu" id="status-menu-<%= disp.id_dispositivo %>">
                                            <button style="color:var(--main-green)" class="status-option-btn" onclick="changeDeviceStatus('<%= disp.id_dispositivo %>', 'livre')">Livre</button>
                                            <button style="color:var(--alert-pink)" class="status-option-btn" onclick="changeDeviceStatus('<%= disp.id_dispositivo %>', 'em_uso')">Em Uso</button>
                                            <button style="color:var(--off-gray)" class="status-option-btn" onclick="changeDeviceStatus('<%= disp.id_dispositivo %>', 'inoperante')">Inoperante</button>
                                        </div>

                                        <form action="/laboratorios/<%= lab.id %>/dispositivos/<%= disp.id_dispositivo %>/delete" method="POST" onsubmit="return confirm('ATENÇÃO ADMIN:\n\nTem certeza que deseja EXCLUIR permanentemente o dispositivo \'<%= disp.nome %>\'?');">
                                            <button type="submit" class="tooltip-btn btn-delete-device">Excluir</button>
                                        </form>
                                    <% } %>
                                </div>
                            </div>
                        </div>
                    
                    <% }); %>
                <% } else { %>
                    <!-- Se não houver dispositivos, exibe mensagem ocupando toda a grade -->
                    <p style="grid-column: 1 / -1; text-align: center; color: #666; font-size: 1.2rem; margin-top: 50px;">
                        [ SISTEMA: Nenhum dispositivo detectado neste setor. ]
                    </p>
                <% } %>
            </div>
        </div>
    </main>

    <%- include('partials/footer') %> 

    <script>
        // --- FUNÇÕES DE STATUS (AJAX) ---
        
        // Alternar visualização do menu de status do laboratório
        function toggleLabMenu() {
            const menu = document.getElementById('labStatusMenu');
            if (menu) {
                menu.classList.toggle('active');
            }
        }

        // Alternar visualização do menu de status do dispositivo
        function toggleStatusMenu(deviceId) {
            const menu = document.getElementById(`status-menu-${deviceId}`);
            // Fecha outros menus se houver
            document.querySelectorAll('.status-options-menu').forEach(el => {
                if (el !== menu) el.style.display = 'none';
            });
            
            if (menu.style.display === 'block') {
                menu.style.display = 'none';
            } else {
                menu.style.display = 'block';
            }
        }

        // Alterar status do dispositivo
        async function changeDeviceStatus(deviceId, newStatus) {
            const idLab = "<%= lab.id %>";
            try {
                const response = await fetch(`/laboratorios/${idLab}/dispositivos/${deviceId}/status`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ status: newStatus })
                });

                const result = await response.json();

                if (result.success) {
                    // Atualizar UI
                    const deviceEl = document.getElementById(deviceId);
                    
                    // Remover classes antigas
                    deviceEl.classList.remove('livre', 'em_uso', 'inoperante');
                    // Adicionar nova
                    deviceEl.classList.add(newStatus);
                    
                    // Atualizar texto
                    const label = deviceEl.querySelector('.status-label');
                    label.innerText = newStatus.replace('_', ' ');

                    // Fechar menu
                    document.getElementById(`status-menu-${deviceId}`).style.display = 'none';
                } else {
                    alert("Erro ao atualizar status.");
                }
            } catch (err) {
                console.error(err);
                alert("Erro de conexão.");
            }
        }

        // Alterar status do Laboratório
        async function changeLabStatus(newStatus) {
            const idLab = "<%= lab.id %>";
            try {
                const response = await fetch(`/laboratorios/${idLab}/status`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ status: newStatus })
                });

                const result = await response.json();

                if (result.success) {
                    const badge = document.getElementById('labStatusBadge');
                    const badgeText = document.getElementById('labStatusText');
                    
                    // Resetar classes
                    badge.classList.remove('disponivel', 'ocupado', 'manutencao', 'inativo');
                    // Add nova classe
                    badge.classList.add(newStatus);
                    
                    // Atualizar texto
                    badgeText.innerText = newStatus.replace('_', ' ').toUpperCase();

                    // Fechar o menu
                    toggleLabMenu();
                    
                } else {
                    alert("Erro ao alterar status do laboratório.");
                }

            } catch (err) {
                console.error(err);
                alert("Erro de conexão.");
            }
        }

        // --- FUNÇÕES DA GALERIA ---
        const modalGaleria = document.getElementById("modalGaleria");
        const modalImagem = document.getElementById("modalImagemExpandida");
        const imgExpandida = document.getElementById("imgExpandida");

        function openGalleryModal() {
            modalGaleria.style.display = "block";
        }

        function closeGalleryModal() {
            modalGaleria.style.display = "none";
        }

        function expandImage(src) {
            imgExpandida.src = src;
            modalImagem.style.display = "flex";
        }

        function closeExpandedImage() {
            modalImagem.style.display = "none";
            imgExpandida.src = "";
        }

        // --- VARIÁVEIS DE CONTROLE DE EDIÇÃO ---
        let isEditMode = false;
        let draggedElement = null;
        
        // Armazena a posição de origem para o SWAP
        let sourceRow = null;
        let sourceCol = null;

        const topologyMap = document.getElementById('topologyMap');

        // --- LÓGICA DE DRAG AND DROP (TOPOLOGIA) ---
        
        function toggleEditMode() {
            isEditMode = !isEditMode;
            const btn = document.getElementById('btnToggleEdit');
            const btnText = document.getElementById('btnEditText');
            const devices = document.querySelectorAll('.device');

            if (isEditMode) {
                // ATIVAR MODO EDIÇÃO
                topologyMap.classList.add('editing');
                btn.classList.add('saving');
                btnText.innerText = '💾 SALVAR MATRIZ';
                
                // Habilitar arraste nos dispositivos
                devices.forEach(dev => {
                    dev.setAttribute('draggable', 'true');
                    addDragEvents(dev);
                });

                // Habilitar drop no mapa
                addMapDropEvents();

            } else {
                // SALVAR E SAIR
                saveTopology();
            }
        }

        async function saveTopology() {
            const devices = document.querySelectorAll('.device');
            const positions = [];

            devices.forEach(dev => {
                // Pega o estilo computado final (após o drop)
                const style = window.getComputedStyle(dev);
                const row = style.gridRowStart;
                const col = style.gridColumnStart;
                
                // Salvar apenas se tiver posição válida numérica
                if (row && col && !isNaN(parseInt(row)) && !isNaN(parseInt(col))) {
                    positions.push({
                        id_dispositivo: dev.id,
                        row: parseInt(row),
                        col: parseInt(col)
                    });
                }
            });

            // Feedback visual de salvamento
            const btnText = document.getElementById('btnEditText');
            btnText.innerText = 'SALVANDO...';

            try {
                const idLab = "<%= lab.id %>";
                const response = await fetch(`/laboratorios/${idLab}/salvar_topologia`, {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ posicoes: positions })
                });

                const result = await response.json();

                if (result.success) {
                    // DESATIVAR MODO EDIÇÃO
                    const btn = document.getElementById('btnToggleEdit');
                    topologyMap.classList.remove('editing');
                    btn.classList.remove('saving');
                    btnText.innerText = '📐 Editar Topologia';

                    // Remover eventos de drag
                    devices.forEach(dev => {
                        dev.setAttribute('draggable', 'false');
                        removeDragEvents(dev);
                    });
                    
                } else {
                    alert("Erro ao salvar: " + result.message);
                    btnText.innerText = '💾 TENTAR NOVAMENTE';
                    isEditMode = true; // Mantém no modo edição
                }
            } catch (err) {
                console.error(err);
                alert("Erro de conexão ao salvar.");
                btnText.innerText = '💾 TENTAR NOVAMENTE';
            }
        }

        // --- EVENTOS DE DRAG NATIVE HTML5 ---

        function addDragEvents(element) {
            element.addEventListener('dragstart', handleDragStart);
            element.addEventListener('dragend', handleDragEnd);
        }

        function removeDragEvents(element) {
            element.removeEventListener('dragstart', handleDragStart);
            element.removeEventListener('dragend', handleDragEnd);
        }

        function handleDragStart(e) {
            draggedElement = this;
            
            // Captura a posição original (row/col) antes de arrastar
            // Usamos getComputedStyle para garantir que pegamos o valor real do grid
            const style = window.getComputedStyle(this);
            sourceRow = parseInt(style.gridRowStart);
            sourceCol = parseInt(style.gridColumnStart);

            e.dataTransfer.effectAllowed = 'move';
            this.style.opacity = '0.4';
            this.style.zIndex = '1000'; // Trazer para frente
        }

        function handleDragEnd(e) {
            this.style.opacity = '1';
            this.style.zIndex = '';
            draggedElement = null;
            sourceRow = null;
            sourceCol = null;
        }

        function addMapDropEvents() {
            topologyMap.addEventListener('dragover', handleDragOver);
            topologyMap.addEventListener('drop', handleDrop);
        }

        function handleDragOver(e) {
            if (e.preventDefault) {
                e.preventDefault(); // Necessário para permitir o drop
            }
            e.dataTransfer.dropEffect = 'move';
            return false;
        }

        function handleDrop(e) {
            if (e.stopPropagation) {
                e.stopPropagation();
            }

            if (draggedElement) {
                // CALCULAR CÉLULA DO GRID BASEADO NA POSIÇÃO DO MOUSE
                const rect = topologyMap.getBoundingClientRect();
                const offsetX = e.clientX - rect.left;
                const offsetY = e.clientY - rect.top;

                // Grid-auto-rows: 80px
                // Colunas: 1fr (total / 12)
                const colWidth = rect.width / 12; 
                const rowHeight = 80; 

                // Calcula índice (mínimo 1, máximo 12)
                let targetCol = Math.max(1, Math.min(12, Math.ceil(offsetX / colWidth)));
                let targetRow = Math.max(1, Math.ceil(offsetY / rowHeight));

                // LÓGICA DE SWAP (TROCA)
                // Verifica se já existe alguém na posição alvo
                const allDevices = document.querySelectorAll('.device');
                let existingDevice = null;

                allDevices.forEach(dev => {
                    if (dev === draggedElement) return;

                    const style = window.getComputedStyle(dev);
                    const r = parseInt(style.gridRowStart);
                    const c = parseInt(style.gridColumnStart);

                    if (r === targetRow && c === targetCol) {
                        existingDevice = dev;
                    }
                });

                if (existingDevice) {
                    // SE EXISTIR DISPOSITIVO NO ALVO, ELE VAI PARA A POSIÇÃO ORIGINAL DO ARRASTADO
                    // Garante que temos valores válidos para a origem
                    const safeSourceRow = isNaN(sourceRow) ? 1 : sourceRow;
                    const safeSourceCol = isNaN(sourceCol) ? 1 : sourceCol;

                    existingDevice.style.gridArea = `${safeSourceRow} / ${safeSourceCol}`;
                }

                // Move o elemento arrastado para o alvo
                draggedElement.style.gridArea = `${targetRow} / ${targetCol}`;
            }

            return false;
        }

        // --- LÓGICA DO MODAL ---
        const modal = document.getElementById("modalDispositivo");
        
        function openModal() {
            if(modal) modal.style.display = "block";
        }

        function closeModal() {
            if(modal) modal.style.display = "none";
        }

        window.onclick = function(event) {
            if (event.target == modal) {
                closeModal();
            }
            if (event.target == modalGaleria) {
                closeGalleryModal();
            }
        }

        // --- LÓGICA DE IMPORTAÇÃO CSV ---
        function handleFileSelect(event) {
            const file = event.target.files[0];
            if (!file) return;

            const reader = new FileReader();

            reader.onload = function(e) {
                const text = e.target.result;
                processCSV(text);
            };

            reader.readAsText(file);
        }

        function processCSV(csvText) {
            const lines = csvText.trim().split(/\r\n|\n/);
            
            if (lines.length < 2) {
                alert("Erro: Arquivo CSV vazio ou inválido.");
                return;
            }

            const dispositivos = [];

            for (let i = 1; i < lines.length; i++) {
                const row = lines[i].split(',');
                
                if (row.length >= 3) {
                    const nome = row[0].trim();
                    const tipo = row[1].trim();
                    const status = row[2].trim();

                    if (nome && tipo) {
                        dispositivos.push({ nome, tipo, status });
                    }
                }
            }

            if (dispositivos.length === 0) {
                alert("Nenhum dispositivo válido encontrado no CSV.");
                return;
            }

            sendDataToServer(dispositivos);
        }

        async function sendDataToServer(data) {
            const idLab = "<%= lab.id %>";
            
            try {
                const response = await fetch(`/laboratorios/${idLab}/importar`, {
                    method: 'POST',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ dispositivos: data })
                });

                const result = await response.json();

                if (result.success) {
                    alert("Importado com sucesso!");
                    window.location.reload(); 
                } else {
                    alert("Erro ao importar: " + result.message);
                }

            } catch (error) {
                console.error("Erro:", error);
                alert("Erro de conexão ao tentar importar.");
            }
        }
    </script>

</body>
</html>